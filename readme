Thomas Parangelo & Matthew Nieto

project can be compiled using "make" and ran using
./submission "input-file" "output-file"

Example: "./submission input1.txt output.txt"

the input files can be changed to any lowercase letters to test
out different inputs as long as the follow the same format
(make sure no extra spaces in end of line)